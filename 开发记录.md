# 九阴全服公示期角色查询书签 - 开发记录

## 项目背景
### 原始需求
用户需要在九阴真经集市（jishi.woniu.com）上，将所有区服的公示期角色账号聚合到一个浏览器书签脚本中，对比当前挂牌价与该角色的最近历史成交价，方便购买高性价比账号。

### 核心目标
- 不用切换服务器，一次看全所有区的公示期角色
- 自动对比每个角色当前价和历史成交价，找出降价/涨价的角色
- 实时运行，不用做静态报告
- 交付形态：浏览器书签脚本（Bookmarklet）

---

## 技术方案演进
### 为什么选书签脚本？
最初尝试过做独立HTML页面，失败原因：
1. **跨域问题**：本地HTML打开时，浏览器不允许直接调用jishi.woniu.com的API（CORS拦截）
2. **混合内容拦截**：HTTPS页面调用HTTP API会被浏览器直接拦截

最终方案：在原网站页面上注入JS脚本（书签形式），同源调用API，无跨域问题，零安装、即开即用。

---

## 核心API端点
所有API均为同源调用，无需鉴权：

| 接口 | 用途 | 参数 |
|------|------|------|
| `/jiuyin/loadServerList.do?gameId=10` | 获取所有服务器列表 | gameId固定为10 |
| `/jiuyin/anonymous/findNoticeGoods.do?serverId={id}&gameId=10&pageIndex={page}` | 获取单个服务器的公示期物品列表 | serverId、pageIndex |
| `/jiuyin/historyTrades.do?serverId={id}&gameId=10&id={roleId}` | 获取单个角色的历史成交记录 | serverId、角色ID |
| `/jiuyin/anonymous/getTradeItem.do?itemId={roleId}&serverId={id}&gameId=10` | 获取角色完整详情数据 | itemId、serverId |

### 关键数据结构
- 角色类型标识：`itemType === '3000'` 表示是角色（不是物品）
- 角色数据字段：id（角色ID）、itemName（角色名）、price（当前价）、grade（等级）、guild（门派）、iconPath（头像）、publicityEndDate（公示结束时间）、marks（标签数组）、gender（性别）
- 历史成交数据：`data[0].data` 数组，按时间倒序，`trades[0]`是最近一次成交

### 原生详情弹窗调用方式
```javascript
tradeItem.show('notice', tradeItemData, {
    id: roleId, 
    N_ID: roleId, 
    reviewStatus: 'notice'
});
```
`tradeItem`是原网站全局对象，直接调用即可弹出原生角色详情窗口。

---

## 已实现功能清单
### 数据层
- [x] 自动遍历所有158个服务器
- [x] 自动翻页获取每个服务器的全部公示期角色
- [x] 批量获取每个角色的历史成交价（并发请求，性能优化）
- [x] 计算当前价与上次成交价的涨跌幅、差额
- [x] 计算最近3次成交价走势

### UI层
- [x] 右侧深色侧边栏面板
- [x] 角色卡片：头像、名称、当前价、涨跌幅、上次成交价、差额、等级、剩余时间、彩色标签
- [x] 顶部筛选栏：服务器、门派、等级、关键词搜索、价格区间、排序、自动刷新
- [x] 价格分布统计栏
- [x] 右键菜单：打开详情、收藏/取消、忽略、复制角色名

### 交互层
- [x] 收藏功能：localStorage存储，收藏角色置顶显示
- [x] 忽略功能：localStorage存储，隐藏不感兴趣的角色
- [x] 记住筛选偏好：localStorage存储
- [x] 导出CSV功能
- [x] 自动刷新（5/10分钟可选）
- [x] 关闭面板后侧边显示红色小竖条，点击重新打开
- [x] 面板左右拖动，边缘自动吸附
- [x] 降价20%以上绿色高亮提醒
- [x] 快到期（1天内）橙色边框提醒
- [x] 精确倒计时显示（X天X时X分）

### 视觉细节
- [x] 彩色标签：装备类紫色、内功经脉类蓝色、武学技能类绿色
- [x] 整体字体大小适配，重点数据加粗放大

---

## 遇到的问题与解决方案
### 1. 收藏/忽略功能调用失败
**问题**：右键菜单点击时提示函数未定义
**原因**：函数写在闭包内，全局访问不到
**解决**：将`toggleFav`、`toggleIgnore`等函数挂到`window`全局对象上

### 2. 角色详情弹窗被面板遮挡
**问题**：打开详情时弹窗在面板下面，看不到
**解决**：将面板z-index从9999降到99，让原生弹窗层级更高，自动显示在面板上面

### 3. 公示剩余时间排序错误
**问题**：只按天数排序，同一天内的小时分钟没考虑
**解决**：直接比较`publicityEndDate`时间戳，精确到秒排序

### 4. 拖动后面板关闭失效
**问题**：拖动后面板用left定位，再设right=-600px无效（left优先级更高）
**解决**：关闭时直接设置`panel.style.left = window.innerWidth + 'px'`，重新打开时清空left、用right定位

### 5. 点关闭按钮触发拖动
**问题**：整个标题栏都绑了mousedown，点按钮也开始拖动
**解决**：mousedown事件里判断，如果点击目标是BUTTON/INPUT/SELECT，不启动拖动

---

## 代码结构说明
书签脚本是一个IIFE，整体结构：
1. URL判断：不在公示期页面则自动跳转
2. DOM创建：创建面板、侧边小条、右键菜单
3. 全局函数挂载：openRoleDetail、toggleFav、toggleIgnore挂到window
4. 数据获取：
   - `getAllNoticeRoles(server)`：获取单个服务器的所有公示角色
   - `loadAllData()`：主函数，遍历所有服务器拉取数据，并发获取历史成交
5. UI渲染：
   - `initFilters()`：初始化筛选下拉框，恢复上次筛选
   - `renderStats()`：渲染价格分布统计
   - `getFilteredRoles()`：根据筛选条件过滤、排序
   - `renderRoles()`：渲染角色卡片列表
6. 事件绑定：筛选、排序、刷新、右键菜单、拖动逻辑

### localStorage存储项
- `jyFavorites`：收藏角色ID数组
- `jyIgnored`：忽略角色ID数组
- `jyFilters`：筛选偏好对象（server/sect/grade/search/minPrice/maxPrice/sortBy）

---

## 后续可扩展功能
### 已规划但未实现
1. **性别筛选**：数据里已有gender字段，加个下拉框即可
2. **常用标签筛选**：太极拳、单脉、全套翡翠等一键筛选开关
3. **详细属性筛选**：内功研修、武学研修、气血、封劲、会心（需要额外调用getTradeItem.do获取每个角色详情，会慢）
4. **数据本地缓存**：缓存上次拉取的数据，下次打开先显示缓存，后台更新
5. **新上架角色标记**：对比上次数据，新出现的角色标红
6. **收藏/忽略数据导出导入**：方便备份和换设备
7. **降价提醒**：后台自动监控，收藏角色降价时桌面通知

### 浏览器插件版升级方向
如果后续要做成Chrome插件：
- 可以加后台自动刷新、桌面通知
- 可以跨页面常驻，不用每次点书签
- 可以同步多设备数据
- 可以加更复杂的筛选规则

---

## 维护注意事项
1. 所有代码都在`bookmarklet.js`一个文件里，是压缩后的单行JS
2. 修改后直接替换书签里的内容即可
3. 不要把面板z-index设太高，会挡住原网站的弹窗
4. 历史成交数据请求比较多（每个角色一次），注意并发数量，不要把服务器打挂
